source "http://rubygems.org"

gem 'jekyll', '~>1.4.0'
gem 'kramdown'

# Required to import posts from wordpress
gem 'sequel'
gem 'yajl-ruby'
gem 'activesupport'
gem 'reverse_markdown'
gem 'mysql'
gem 'hpricot'

# Notes
# If running on ubuntu install
# sudo apt-get install libmysqlclient-dev
#
# before running bundle install
